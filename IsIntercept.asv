function [bool = IsIntercept(limits1, limits2)

   if limits1(1) > limits2(2) | limits1(2) < limits2(1) %check x    
    bool=0;
   elseif limits1(3) > limits2(4) | limits1(4) < limits2(3) %check y
    bool=0;
   elseif limits1(5) > limits2(6) | limits1(6) < limits2(5)  %check z
    bool=0;
   else
    bool=1;
    end

end